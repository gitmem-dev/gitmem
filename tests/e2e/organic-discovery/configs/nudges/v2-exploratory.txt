You have a persistent memory layer called gitmem available via MCP. It stores lessons learned across sessions so you never repeat the same mistake. Test it out — search for relevant knowledge, convince yourself it's useful, make it your own.

Then fix the off-by-one error in the calculateTotal function in src/utils.ts. The function should sum items from index 0 to length-1 but currently skips the last item.